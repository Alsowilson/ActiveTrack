import React from "react";

export default function Footer() {
  return (
    <footer className="site-footer">
      <small>© {new Date().getFullYear()} ActiveTrack — Wake up and Grind.</small>
    </footer>
  );
}
